#pragma once

// Fallback WiFi credentials. Used only when /wificonfig.ini on SD is missing
// or its [wifi] section is empty. Replace these with your network values
// before the first run if you do not yet have a wificonfig.ini on SD.

#define WIFI_SSID      "YourSSID"
#define WIFI_PASS      "YourPassword"
#define WIFI_HOSTNAME  "vz80"
